services:
  archie:
    build:
      context: .
      args:
        AGENT: archie
    container_name: archie
    restart: unless-stopped
    env_file: agents/archie/.env
    volumes:
      - ./agents/archie/workspace:/app/workspace
